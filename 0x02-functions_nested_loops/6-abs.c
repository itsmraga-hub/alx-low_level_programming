#include "main.h"

/**
* _abs - function to print the absolute value of integer
* @n: integer to be returned as abs
*
* Return: (0) always
*/

int _abs(int n)
{
	if (n > 0)
		return (n);
	
	return (-n);
}
